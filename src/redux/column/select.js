export const selectColumns = state => state.controlers.columns;

export const selectCurrentColumn = state => state.controlers.currentColumn;

export const selectIsLoading = state => state.controlers.isLoading;

export const selectError = state => state.controlers.error;

export const selectIsAddColumnOpen = state => state.controlers.isAddColumnOpen;

export const selectIsEditColumnOpen = state => state.controlers.isEditColumnOpen;
