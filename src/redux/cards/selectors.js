export const selectCards = state => state.controlers.cards;

export const selectCurrentCard = state => state.controlers.currentCard;

export const selectIsLoading = state => state.controlers.isLoading;

export const selectError = state => state.controlers.error;

export const selectIsAddCardOpen = state => state.controlers.isAddCardOpen;

export const selectIsEditCardOpen = state => state.controlers.isEditCardOpen;
